# NPM 配置文件
# 用于确保 Netlify 构建时的依赖解析正确性

# 提升依赖到顶层，确保所有模块都能被找到
shamefully-hoist=true

# 启用严格的对等依赖检查
strict-peer-deps=false

# 设置注册表
registry=https://registry.npmjs.org/

# 禁用包锁定文件的自动生成（因为我们使用 pnpm）
package-lock=false

# 设置日志级别
loglevel=warn

# 禁用进度条以减少日志噪音
progress=false

# 设置超时时间
fetch-timeout=300000
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
